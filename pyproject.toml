[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "mathub-material-extractor"
version = "0.3.2"
description = "Deterministic PDF ingestion and zero-API lesson localization for the Mathub material pipeline."
readme = "README.md"
requires-python = ">=3.12,<3.15"
dependencies = [
    "PyMuPDF>=1.26,<1.29",
]

[project.optional-dependencies]
dev = [
    "pytest>=8,<9",
]

[project.scripts]
mathub-extract = "mathub_extractor.cli:main"
mathub-locate = "mathub_extractor.locator_cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

[tool.setuptools.packages.find]
where = ["src"]
